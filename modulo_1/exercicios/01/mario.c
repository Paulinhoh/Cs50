#include <stdio.h>
#include <cs50.h>

int tijolos(int tam)
{
    for (int i = 0; i < tam; i++)
    {
        for (int j = 0; j < tam; j++)
        {
        }
    }
}

int main(void)
{
    int tamanho = get_int("Tamanho: ");
    tijolos(tamanho);
}
